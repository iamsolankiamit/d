<div class="panel-heading">
<div class="row">
<div class="col-sm-4">{{#link-to "playlists" class="btn btn-primary "}}Back to all Playlists{{/link-to}}</div>
<div class="col-sm-4"><h4 class="text-center">{{model.name}}</h4></div>
<div class="col-sm-4 text-right"><button class="btn btn-primary">Save</button></div>
</div>
</div>
<div class="panel-body">
	
	{{#each video in model.videos}}

	{{video.movie_file_name}}

	{{/each}}

	{{outlet}}
</div>
